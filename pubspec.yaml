name: plagas_arroz
description: "Sistema de detección de plagas"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.12.1
  sqflite: ^2.3.0
  path: ^1.9.0
  http: ^1.2.0
  image_picker: ^1.0.7
  geolocator: ^14.0.2
  image: ^4.2.0
  path_provider: ^2.1.5
  pdf: ^3.12.0
  share_plus: ^12.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
      - assets/models/
